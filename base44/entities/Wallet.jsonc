{
  "name": "Wallet",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "description": "Optional notes or description for this wallet record",
      "maxLength": 1000
    },
    "user_id": {
      "type": "string",
      "description": "Unique user ID (index)"
    },
    "free_balance": {
      "type": "number",
      "default": 0
    },
    "paid_balance": {
      "type": "number",
      "default": 0
    },
    "last_free_accrual_date": {
      "type": "string",
      "description": "YYYY-MM-DD UTC, nullable"
    },
    "version": {
      "type": "number",
      "default": 1,
      "description": "Optimistic concurrency version"
    }
  },
  "required": [
    "user_id"
  ]
}